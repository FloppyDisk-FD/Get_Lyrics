name: Build & Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

jobs:
  build-ffmpeg:
    name: Build FFmpeg (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            platform: linux-x64
            exe: ffmpeg
          - os: macos-latest
            platform: macos-x64
            exe: ffmpeg
          - os: windows-latest
            platform: win-x64
            exe: ffmpeg.exe

    steps:
      - uses: actions/checkout@v4

      - name: Install build dependencies (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential yasm nasm pkg-config curl xz-utils

      - name: Install build dependencies (macOS)
        if: runner.os == 'macOS'
        run: |
          brew install yasm nasm pkg-config

      - name: Install build dependencies (Windows)
        if: runner.os == 'Windows'
        shell: bash
        run: |
          choco install yasm nasm --no-progress -y

      - name: Build FFmpeg
        shell: bash
        run: bash scripts/build-ffmpeg.sh

      - name: Package binary
        shell: bash
        run: |
          mkdir -p dist/ffmpeg-${{ matrix.platform }}
          cp bin/${{ matrix.exe }} dist/ffmpeg-${{ matrix.platform }}/
          cd dist
          if [[ "${{ runner.os }}" == "Windows" ]]; then
            7z a -ttar ffmpeg-${{ matrix.platform }}.tar ffmpeg-${{ matrix.platform }}/
            7z a -tgzip ffmpeg-${{ matrix.platform }}.tar.gz ffmpeg-${{ matrix.platform }}.tar
          else
            tar -czf ffmpeg-${{ matrix.platform }}.tar.gz ffmpeg-${{ matrix.platform }}/
          fi
          ls -lh ffmpeg-${{ matrix.platform }}.tar.gz

      - uses: actions/upload-artifact@v4
        with:
          name: ffmpeg-${{ matrix.platform }}
          path: dist/ffmpeg-${{ matrix.platform }}.tar.gz

  build-get-lyrics:
    name: Build get-lyrics (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            platform: linux-x64
            exe: get-lyrics
          - os: macos-latest
            platform: macos-x64
            exe: get-lyrics
          - os: windows-latest
            platform: win-x64
            exe: get-lyrics.exe

    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install

      - name: Build get-lyrics binary
        shell: bash
        run: |
          bun build index.ts --compile --outfile ${{ matrix.exe }}
          ls -lh ${{ matrix.exe }}

      - name: Package binary
        shell: bash
        run: |
          mkdir -p dist/get-lyrics-${{ matrix.platform }}
          cp ${{ matrix.exe }} dist/get-lyrics-${{ matrix.platform }}/
          cd dist
          if [[ "${{ runner.os }}" == "Windows" ]]; then
            7z a -ttar get-lyrics-${{ matrix.platform }}.tar get-lyrics-${{ matrix.platform }}/
            7z a -tgzip get-lyrics-${{ matrix.platform }}.tar.gz get-lyrics-${{ matrix.platform }}.tar
          else
            tar -czf get-lyrics-${{ matrix.platform }}.tar.gz get-lyrics-${{ matrix.platform }}/
          fi
          ls -lh get-lyrics-${{ matrix.platform }}.tar.gz

      - uses: actions/upload-artifact@v4
        with:
          name: get-lyrics-${{ matrix.platform }}
          path: dist/get-lyrics-${{ matrix.platform }}.tar.gz

  release:
    name: Create Release
    needs: [build-ffmpeg, build-get-lyrics]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: dist

      - name: Display structure
        run: ls -R dist/

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: dist/**/*.tar.gz
          generate_release_notes: true
